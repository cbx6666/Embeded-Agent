# 可选：摄像头 + MediaPipe +（可选）PyTorch RAF 权重
# pip install -r requirements.txt -r requirements-vision.txt
-r requirements.txt

opencv-python-headless>=4.8.0
mediapipe>=0.10.9

# 情绪分类（仅当提供 --raf-ckpt 时需要）
torch>=2.0.0
torchvision>=0.15.0
